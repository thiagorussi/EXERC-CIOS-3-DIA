/* let somarValores = function (n1,n2) {
    let total = 0
    total = n1 + n2
    return total
}

alert (`A soma dos valores é : ${somarValores(10,20)}`) */

let subtrairValores = (n1,n2) => {
    if(typeof n1 == 'string'){
    } else {
        alert ('parâmetro do tipo inválido')
    }
    return n1-n2
}